package com.example.demo.repository;

import com.example.demo.entity.Eu;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EuRepository extends JpaRepository<Eu, Long> {
}
